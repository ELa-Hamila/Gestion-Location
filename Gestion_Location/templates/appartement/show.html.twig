{% extends 'base.html.twig' %}

{% block title %}Appartement{% endblock %}

{% block body %}
    <h1>Appartement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appartement.id }}</td>
            </tr>
            <tr>
                <th>Localite</th>
                <td>{{ appartement.localite }}</td>
            </tr>
            <tr>
                <th>NbPiece</th>
                <td>{{ appartement.nbPiece }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ appartement.valeur }}</td>
            </tr>
            <tr>
                <th>Image1</th>
                <td>{{ appartement.image1 }}</td>
            </tr>
            <tr>
                <th>Image2</th>
                <td>{{ appartement.image2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_appartement_index') }}">back to list</a>

    <a href="{{ path('app_appartement_edit', {'id': appartement.id}) }}">edit</a>

    {{ include('appartement/_delete_form.html.twig') }}
{% endblock %}
